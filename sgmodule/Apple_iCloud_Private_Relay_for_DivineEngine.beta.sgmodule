#!name= Unlock iCloud Private Relay
#!desc=(BETA) 启用iCloud专用代理，需要Surge 共享网络连接/启用网关模式 给终端设备

[General]
# ---(通用)---
# 通用设置
# 隐藏状态栏上的 VPN 图标
hide-vpn-icon=true

# > TLS 引擎
tls-provider = network-framework
# ------

# ---(实验性功能)---
# > 使用Network framwork
network-framework = true
# ------

# ---(高级)---
# 高级设置

# > 兼容性
# 兼容模式
# Proxy with Loopback Address
# compatibility-mode = 1
# Proxy
# compatibility-mode = 2
# TUN Only
# compatibility-mode = 3

# > 路由
# 包含所有的网络请求
# include-all-networks = true

# Always Real IP Hosts
# 当 Surge VIF 处理 DNS 问题时，此选项要求 Surge 返回一个真正的 IP 地址，而不是一个假 IP 地址。
# DNS 数据包将被转发到上游 DNS 服务器。
always-real-ip = %APPEND% mask.icloud.com, mask.apple-dns.net, mask-h2.icloud.com, mask-t.apple-dns.net

# VIF Excluded Routes
# Surge VIF 只能处理 TCP 和 UDP 协议。使用此选项可以绕过特定的 IP 范围，允许所有流量通过。
# tun-excluded-routes = %APPEND% 127.0.0.1, 127.0.0.0/8

# VIF Included Routes
# 默认情况下，Surge VIF 接口会声明自己是默认路由。但是，由于 Wi-Fi 接口的路由较小，有些流量可能不会通过 Surge VIF 接口。使用此选项可以添加一条较小的路由。
# tun-included-routes = %APPEND% 127.0.0.1, 127.0.0.0/8
# ------

[Proxy]
# 🌐iCloud+ 专用代理
🌐iCloud+ Private Relay = DIRECT, interface=en0, allow-other-interface=true
#🌑Surge TUN
🌑Surge Loopback = DIRECT, interface=lo0, allow-other-interface=true

[Rule]
# > Client
# Safari
# PROCESS-NAME,com.apple.WebKit.Networking,🌐iCloud+ Private Relay
# PROCESS-NAME,/System/Library/Frameworks/WebKit.framework/Versions/A/XPCServices/com.apple.WebKit.Networking.xpc/Contents/MacOS/com.apple.WebKit.Networking,DIRECT
USER-AGENT,AppleWebKit*,🌐iCloud+ Private Relay

# > REJECT AutoNavi Location Services Dispatcher
# DOMAIN,dispatcher.is.autonavi.com,REJECT

# > Apple
# Location Services
OR,((DOMAIN,gsp64-ssl.ls.apple.com),(IP-CIDR,17.142.169.199/32,no-resolve)),🌑Proxy
# IP-CIDR,17.142.169.199/32,🌑Proxy,no-resolve
# DOMAIN,gsp64-ssl.ls.apple.com,🌑Proxy

# Apple
AND,((PROTOCOL,UDP),(IP-CIDR,17.0.0.0/8,no-resolve),(DEST-PORT,443)),🌐iCloud+ Private Relay
# AND,((PROTOCOL,TCP)(IP-CIDR,17.0.0.0/8,no-resolve),(DEST-PORT,443),),🌑Proxy

# > iCloud 
DOMAIN,gateway.icloud.com,🌑Proxy
DOMAIN,metrics.icloud.com,🌑Proxy
# DOMAIN,p102-quota.icloud.com,🌑Proxy
# iCloud services in China
DOMAIN-SUFFIX,apzones.com,DIRECT
DOMAIN-SUFFIX,icloud.com.cn,DIRECT
# iCloud services
DOMAIN-SUFFIX,icloud-content.com,🌑Proxy
DOMAIN-SUFFIX,icloud-content.com.akadns.net,🌑Proxy
DOMAIN-KEYWORD,content.icloud.com,🌑Proxy
DOMAIN-KEYWORD,content.icloud.com.akadns.net,🌑Proxy

# > Apple Content caching
# https://support.apple.com/en-us/HT210060
IP-CIDR,17.57.21.63/32,🌑Proxy,no-resolve
# Server registration
DOMAIN,lcdn-registration.apple.com,🌑Proxy
# Content caching locator service
DOMAIN,lcdn-locator.apple.com,🌑Proxy

# > iCloud Private Relay
# https://developer.apple.com/cn/support/prepare-your-network-for-icloud-private-relay/
# https://mask-api.icloud.com/egress-ip-ranges.csv

# Optimize for Private Relay connections
AND,((PROTOCOL,UDP),(DOMAIN,mask-api.icloud.com),(DEST-PORT,443)),🌐iCloud+ Private Relay
AND,((PROTOCOL,UDP),(DOMAIN,mask.icloud.com),(DEST-PORT,443)),🌐iCloud+ Private Relay
AND,((PROTOCOL,UDP),(DOMAIN,mask.apple-dns.net),(DEST-PORT,443)),🌐iCloud+ Private Relay
AND,((PROTOCOL,UDP),(DOMAIN,mask-h2.icloud.com)),(DEST-PORT,443),🌐iCloud+ Private Relay
AND,((PROTOCOL,UDP),(DOMAIN,mask-t.apple-dns.net),(DEST-PORT,443)),🌐iCloud+ Private Relay

# Allow for network traffic audits
AND,((PROTOCOL,TCP),(DOMAIN,mask-api.icloud.com),(DEST-PORT,443)),🌑Proxy
AND,((PROTOCOL,TCP),(DOMAIN,mask.icloud.com),(DEST-PORT,443)),🌑Proxy
AND,((PROTOCOL,UDP),(DOMAIN,mask.apple-dns.net),(DEST-PORT,443)),🌑Proxy
AND,((PROTOCOL,TCP),(DOMAIN,mask-h2.icloud.com),(DEST-PORT,443)),🌑Proxy
AND,((PROTOCOL,UDP),(DOMAIN,mask-t.apple-dns.net),(DEST-PORT,443)),🌑Proxy